import SectionTitle from "../SectionTitle";

const Detail = () => {
  return (
    <main className="font-chakra w-full pt-48">
      <SectionTitle section="Detail Solution" />
    </main>
  );
};

export default Detail;
